name: Publish Package to GPR

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        run: npm ci

      - name: Publish to GPR
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: 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
